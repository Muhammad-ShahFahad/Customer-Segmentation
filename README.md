# Customer-Segmentation
Customer segmentation project using K-Means clustering on RFM (Recency, Frequency, Monetary) + Income + Age features. Includes data cleaning, outlier handling, cluster validation via the elbow method, PCA visualization, and segment profiling to generate actionable, targeted marketing recommendations for each customer group.
